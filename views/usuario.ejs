<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MusicApp - Inicio</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" />
  <link rel="stylesheet" href="/css/estilos.css" />
</head>
<body>
  <!-- Barra de navegación -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/home">MusicApp</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/agregar">Agregar Canción</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">Cerrar sesión</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Contenido principal -->
  <div class="container mt-5">
    <h2>Bienvenido, <%= user.username %>!</h2>
    <p>Explora, guarda y escucha tus canciones favoritas.</p>

    <!-- Lista de canciones -->
    <h3 class="mt-4">Lista de Canciones</h3>

    <% if (canciones.length === 0) { %>
      <p class="text-muted">No hay canciones disponibles.</p>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-3 g-4 mt-3">
        <% canciones.forEach(cancion => { %>
          <div class="col">
            <div class="card h-100">
              <img src="https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?fit=crop&w=800&q=80" class="card-img-top" alt="Portada canción" />
              <div class="card-body">
                <h5 class="card-title"><%= cancion.nombre %></h5>
                <p class="card-text"><strong>Artista:</strong> <%= cancion.artista %></p>
                <p class="card-text"><strong>Género:</strong> <%= cancion.genero %></p>

                <% if (cancion.archivo) { %>
                  <audio controls style="width: 100%;">
                    <source src="/<%= cancion.archivo %>" type="audio/mpeg" />
                    Tu navegador no soporta la reproducción de audio.
                  </audio>
                <% } else { %>
                  <p class="text-muted">Audio no disponible</p>
                <% } %>

                <% if (cancion.archivo_letra) { %>
                  <a href="/<%= cancion.archivo_letra %>" target="_blank" class="btn btn-sm btn-outline-primary mt-2">
                    Ver Letra
                  </a>
                <% } else { %>
                  <p class="text-muted mt-2">Letra no disponible</p>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <footer class="footer bg-dark text-light text-center mt-5 py-3">
    <p>&copy; 2025 MusicApp. Todos los derechos reservados.</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
